# 王への請願（ソロ） — 開発プラン & 進捗

> **このファイルの役割**
> セッション（担当者/AI）が代わっても作業を継続できるようにするための「単一の真実」。
> 作業を始めるときはまずこれを読み、作業を終えるときは必ず更新すること。
> 実装の細かい経緯・判断理由は `DEVLOG.md` に残す。

- プロジェクト: `OriginalGames/KingPetition`
- 反映先ブランチ: `main`（利用者の指示により main に直接反映）
- 最終更新: 2026-08-22（ソロで一通り遊べる状態）

---

## 0. いま何待ちか ← 最初に読む

**ソロプレイとして一通り遊べる状態。実機での通し確認とバランス調整が残っている。**

- ルール仕様は **`docs/rules/`** に確定版として整理済み（原典の保存HTMLは取り込み後に削除）
  - `01-overview.md` ターンの流れ / `02-cards.md` カード20種 / `03-endgame.md` 最終ラウンド
  - `04-solo.md` ソロ用ルール（本アプリ独自設計） / `99-faq.md` 実装側で判断した曖昧点
  - `00-source.md` 出典URL（開発環境からは egress 制限で到達不可）
- カード画像20枚は **`assets/cards/`**（ファイル名＝カードID）

### 次担当者の手順
1. `python3 -m http.server 8080` で起動して実際に遊ぶ（`node --test` でロジックの回帰確認）
2. 気になった点は `docs/rules/99-faq.md`（ルール解釈）と `src/game/score.js`（スコア係数）を見る
3. 原典に無い判断を新たにしたら、必ず `docs/rules/99-faq.md` に追記する

---

## 1. 何を作るか

『王への請願』を題材にした **1人用（ソロプレイ）デジタルアプリ**。ブラウザだけで動く。

### ゴール
- 制限ターン内にダイスを振り、条件を満たしてキャラクターカードを獲得していく
- カードの効果でダイスが増え・操作できるようになり、最終的に「王」を獲得する
- 王を獲得したら最終ラウンドを行い、スコアを確定する
- ハイスコアをローカル保存、日替わりシードのチャレンジあり

### 非ゴール（やらないこと）
- オンライン対戦・サーバ通信
- 公式アートワーク／カード画像の使用（表示はすべて自前の CSS / SVG）

---

## 2. 技術方針

| 項目 | 決定 | 理由 |
|---|---|---|
| 言語 | 素の HTML + CSS + JavaScript (ES Modules) | ビルド不要。`node_modules` なしで即動く＝セッションが代わっても再開が容易 |
| 依存 | **ゼロ** | 環境構築の失敗要因を消す |
| テスト | Node 標準 `node --test` | 追加依存なしでロジックを検証 |
| 状態保存 | `localStorage` | ハイスコア・設定 |
| 乱数 | mulberry32 のシード付きRNG | 日替わりチャレンジ＆テストの再現性 |

同リポジトリの既存プロジェクトは Vite 構成だが、本プロジェクトは「継続のしやすさ」を優先して
ビルドなし構成を選んだ（理由は `DEVLOG.md` 2026-08-22 参照）。静的配信なので GitHub Pages も可。

### 起動方法
```bash
cd KingPetition
python3 -m http.server 8080     # もしくは npx serve .
# → http://localhost:8080/
```
（`file://` 直開きは ES Modules の CORS 制限で不可。必ず HTTP サーバ経由）

### テスト実行
```bash
cd KingPetition
node --test test/
```

---

## 3. ファイル構成

```
KingPetition/
├── index.html            画面の骨格
├── package.json          type:module とテスト用スクリプトだけ（依存パッケージは無し）
├── README.md             遊び方・概要
├── PROGRESS.md           ← このファイル（計画と進捗）
├── DEVLOG.md             実装経過ログ（要点・判断理由）
├── assets/
│   ├── favicon.svg
│   └── cards/            ★カード画像20枚（ファイル名＝カードID）+ 対応表 README
├── docs/
│   └── rules/            ★ルール仕様（確定版・実装の唯一の参照先）
│       ├── README.md       索引と実装時の注意
│       ├── 00-source.md    出典と取り込み記録
│       ├── 01-overview.md  目的・準備・ターンの流れ
│       ├── 02-cards.md     カード20種（cards.js の元データ）
│       ├── 03-endgame.md   最終段階・最終ラウンド・勝敗
│       ├── 04-solo.md      ソロ用ルール（独自設計）
│       └── 99-faq.md       実装側で判断した曖昧点
├── src/
│   ├── css/style.css     王宮テーマ
│   ├── game/             ★ロジック（DOM非依存・テスト対象）
│   │   ├── rng.js          シード付き乱数（mulberry32）
│   │   ├── cards.js        カード20種の定義（02-cards.md が元データ）
│   │   ├── requirements.js 出目条件の判定・説明文
│   │   ├── abilities.js    カード能力の効果適用
│   │   ├── engine.js       ゲーム進行のステートマシン
│   │   ├── score.js        スコア計算
│   │   └── storage.js      localStorage ラッパ
│   └── ui/               ★描画・入力（DOM依存）
│       ├── app.js          状態保持・イベント配線・能力の対象選択フロー
│       └── render.js       描画
└── test/                 node --test 用（requirements / engine / abilities）
```

**設計原則: `src/game/` は DOM を一切触らない。**
UI から engine を操作し、返ってきた状態を描画するだけにする。

---

## 4. ルール仕様

**確定済み。`docs/rules/` を唯一の参照先とする。** 以下は実装で特に効く要点だけの抜粋。

### 原典どおりに実装する点
- 初期ダイス **3個**。カードで増える（農夫+1 / ペテン師+1 / 将軍+2 → 最大7個）
- 手番: 全部振る → アクティブなダイスから **最低1つ確定** → 残りを振り直す →
  **全ダイスが確定するまで繰り返す**
  - ⚠️ **「好きなところでストップ」はできない**。ここを取り違えるとゲームが別物になる
- 獲得は1手番 **1枚**、**まだ自分が持っていないもの**。国王を取ると **王妃も同時**に獲得
- 所持カードは **1手番に1回ずつ** 使用可。効果の対象は原則 **未確定のダイス**
- 手番中に未確定ダイスを追加する能力（職人1/衛兵2/狩人3/質屋4/騎士5/司教6/王妃 任意）
  → 追加したぶんターンが伸びる。最大14個まで積める＝これで7ゾロを狙う
- 出目判定は「条件を満たす組み合わせが含まれるか」（1111 は 4つ同じ かつ 2ペア）
- 最終ラウンドの強さ: ①同目数が多い ②同数なら目が大きい ③それも同じなら先に振ったほう

### ソロ化（本アプリ独自 → `docs/rules/04-solo.md`）
- カードは各1枚。制限ラウンド内に国王（7ゾロ）を獲得するのが目標
- 難易度＝制限ラウンド数（やさしい15 / ふつう12 / むずかしい10）
- 国王獲得後に最終ラウンドを1回行い、出目の強さが王冠評価になる
- スコア = カードLV合計×10 ＋ 国王200 ＋ 残ラウンド×25 ＋ 最終ラウンドの最大同目数×15 ＋ その目×1
  （係数は `src/game/score.js` の定数にまとめて調整可能にする）

### 未確定な点
原典に明記がなく実装側で解釈した項目が10件ある → **`docs/rules/99-faq.md`**。
特に「哲学者・錬金術師の対象は未確定ダイスに限るか」「能力の使用タイミング」は要確認。

---

## 5. 開発フェーズと進捗

凡例: `[ ]` 未着手 / `[~]` 作業中 / `[x]` 完了 / `[!]` ブロック中

### フェーズ0: 土台
- [x] ディレクトリ作成
- [x] `PROGRESS.md`（本ファイル）
- [x] `DEVLOG.md`
- [x] `README.md`
- [x] `docs/rules/` の受け入れテンプレ一式
- [x] main に反映

### フェーズ0.5: ルール資料の取り込み
- [x] 利用者から原典（BSW版ルール翻訳 / peca卓ゲwiki カード一覧）を受領
- [x] `01-overview.md` にターンの流れを書き起こし
- [x] `02-cards.md` にカード20種の獲得条件・効果を書き起こし
- [x] `03-endgame.md` に最終段階・勝敗判定を書き起こし
- [x] `04-solo.md` にソロ用ルールを設計（原典にソロバリアントは無し）
- [x] `99-faq.md` に実装側で判断が必要な曖昧点を10件洗い出し
- [x] カード画像20枚を `assets/cards/` へ配置（ファイル名＝カードID）
- [x] 原典の保存HTML（`docs/rules/source/`）を削除
- [x] 本ファイル §4 にルール仕様を確定

### フェーズ1: ゲームロジック（DOM非依存）
- [x] `rng.js` シード付き乱数
- [x] `requirements.js` 出目条件の判定と説明文生成
- [x] `cards.js` カード定義（`docs/rules/02-cards.md` から生成）
- [x] `abilities.js` 能力効果
- [x] `engine.js` ステートマシン（振る／確定／振り直し／獲得／最終ラウンド）
- [x] `score.js` スコア計算
- [x] `test/` ユニットテスト（37件・`node --test` で全件パス）

### フェーズ2: UI
- [x] `index.html` 骨格
- [x] `style.css` 王宮テーマ
- [x] `render.js` ダイス・カード・ログ描画
- [x] `app.js` イベント配線・能力の対象選択フロー
- [x] タイトル/難易度選択・リザルト画面
- [x] ルール説明モーダル

### フェーズ3: 仕上げ
- [x] `storage.js` ハイスコア保存
- [x] 日替わりチャレンジ（日付シード）
- [x] スマホ幅の調整（375px で横スクロール無しを確認）
- [ ] 効果音（任意）

### フェーズ4: 今後の候補
- [ ] オートマ（自動対戦相手）モード
- [ ] リプレイ／棋譜（シード＋操作列の保存と再生）
- [ ] GitHub Pages へのデプロイ（他プロジェクトと同様）

---

## 6. 現在の状況

- フェーズ0〜3 の主要項目まで実装済み。ソロで最初から最後まで遊べる。
- `node --test` は37件すべてパス。
- ヘッドレス Chromium で通し確認済み（手番→獲得→国王→最終ラウンド→リザルト、
  能力の対象選択UI、375px幅のレイアウト）。コンソールエラー無し。
- 未実施: 実機ブラウザでの目視確認、バランス調整（難易度ごとの達成率）。

## 7. 次にやること

1. `python3 -m http.server 8080` で起動し、実際に何回か通しで遊ぶ
2. 難易度ごとの達成しやすさを見て、制限ラウンド数と `src/game/score.js` の係数を調整
3. `docs/rules/99-faq.md` の未解決項目（#9 能力の使用タイミング、#11 獲得しない選択）を
   原典に当たれる機会があれば確認する
4. 余力があれば フェーズ4（オートマ、リプレイ、GitHub Pages デプロイ）

## 8. 用語メモ（コード内で使う呼び名の予定）

| コード上の語 | 意味 |
|---|---|
| `die.locked` | 取り置き済み（このターンもう振り直されない）ダイス |
| `selection` | 今回の振り直しで取り置く候補として選択中のダイス |
| `pendingAbility` | 対象選択待ちの能力（UIが対象を集めて engine に渡す） |
| `phase` | `title` / `roll` / `claim` / `final` / `result` |
