# DEVLOG — 実装経過ログ

計画・進捗は `PROGRESS.md`。ここには **「なぜそうしたか」「何にハマったか」** を時系列で残す。
新しいセッションで作業したら、末尾にエントリを追記すること。

書式:
```
## YYYY-MM-DD — 見出し
### やったこと
### 決めたこと（理由つき）
### 詰まった点 / 未解決
```

---

## 2026-08-22 — プロジェクト立ち上げ

### やったこと
- `OriginalGames/KingPetition/` を新規作成
- `PROGRESS.md`（計画・進捗の単一の真実）と本 `DEVLOG.md` を作成

### 決めたこと（理由つき）
- **ビルドツールを使わない（素の ES Modules）。**
  同リポジトリの既存プロジェクト（MapPuzzle2 / TD-game / SmikkoClicker）は Vite 構成だが、
  今回は「セッションが代わっても続きができる」ことが要件。`npm install` の成否や
  `node_modules` の有無に左右されない構成のほうが再開コストが低いと判断した。
  静的ファイルのみなので GitHub Pages 配信もそのまま可能。
- **`src/game/`（純ロジック）と `src/ui/`（DOM）を厳密に分離。**
  ロジックを Node から直接 import してテストできるようにするため。
- **乱数はシード付き（mulberry32）。**
  テストの再現性を確保しつつ、「日替わりチャレンジ」（日付をシードにする）を無料で得られる。

### 詰まった点 / 未解決
- **公式ルールの一次情報にアクセスできなかった。**
  ネットワークの egress プロキシが BoardGameGeek / Wikipedia / Yucata / Grokipedia を
  いずれもブロック（`EGRESS_BLOCKED`）。検索スニペットから確認できたのは以下のみ:
  - キャラクターは19種（カード60枚）、ダイスは12個
  - 「農夫」はペアで獲得でき、能力は「ダイスを1個追加」
  - 「王」「女王」はレベル5、獲得条件は7ゾロ
  - 7ゾロを最初に出した人が王を得て、最終ラウンドで他プレイヤーが王を奪いに行く
  → **カード19種の詳細は記憶にもとづく再構成**として実装し、
    `src/game/cards.js` 1ファイルを差し替えれば直せる構造にする。
    この制約は `PROGRESS.md` §4 と `README.md` にも明記した。

---

## 2026-08-22 — ルール資料を人手で受け取る方針に変更 / main へ反映

### やったこと
- `docs/rules/` を新設し、ルール資料の受け入れ口を整備
  - `README.md` … 何をどこに置くかの説明
  - `01-overview.md` `02-cards.md` `03-endgame.md` `04-solo.md` `99-faq.md` … 記入テンプレ
  - `source/` … PDF・写真・コピペテキストの生資料置き場（`source/README.md` に注意書き）
- `README.md`（プロジェクト概要）を追加
- `PROGRESS.md` を全面改訂。冒頭に「§0 いま何待ちか」を置き、
  資料待ちでブロックしている状態と、資料が届いた後の手順を明記した
- 作業ブランチではなく **`main` に直接反映**（利用者の明示的な指示による）

### 決めたこと（理由つき）
- **カード内容を推測で実装しない。**
  当初は記憶ベースで19種を再構成して実装する予定だったが、利用者が正式資料を用意すると
  申し出たため中止した。推測で書いたデータは誤りが固定化しやすく、
  あとから直すコストのほうが高い。`PROGRESS.md` にも禁止事項として明記。
- **受け口をテンプレ（記入式）と生資料置き場の二本立てにした。**
  「書き起こすのが面倒」「写真しかない」のどちらでも詰まらないようにするため。
  最優先で欲しい資料は **サマリーカード（カード一覧表）** だと `source/README.md` に書いた。
- **カード定義は `src/game/cards.js` 1ファイルに閉じ込める設計を維持。**
  資料の内容が後から変わっても差し替え1箇所で済むようにするため。
  条件判定（`requirements.js`）も条件タイプを汎用化して、条件変更に耐えるようにする。

### 詰まった点 / 未解決
- egress プロキシにより外部のルール資料へ到達できない状態は変わっていない
  （BGG / Wikipedia / Yucata / Grokipedia すべて `EGRESS_BLOCKED`）。
  npm・PyPI 等のパッケージレジストリのみ通る。今後も一次資料は人手で入れる前提で進める。
- 実装コードは未着手。資料が届き次第フェーズ1から開始する。

---

## 2026-08-22 — 原典ルールの取り込み完了 / ブロック解除

### やったこと
- 利用者から `docs/rules/source/` に原典資料を受領（保存済みWebページ2件＋画像）
  - BSW版『王への請願』ルール翻訳（BlueBear 氏 / Shift_JIS の HTML）
  - peca卓ゲwiki「カード一覧」（カード20種の獲得条件・効果＋肖像画20枚）
- HTMLからテキストを抽出し、`docs/rules/` の各ファイルへ書き起こし
  - `00-source.md`（出典記録）/ `01-overview.md` / `02-cards.md` / `03-endgame.md` /
    `04-solo.md` / `99-faq.md` / `README.md`（索引）
- カード画像20枚を `assets/cards/` へ **カードIDと同名の英字ファイル名** で配置し、対応表を添えた
- `docs/rules/source/` を削除（再利用不要との指示）
- `PROGRESS.md` §0 §4 と `README.md` を確定版ルールに合わせて更新

### 判明した重要ルール（設計に影響が大きい順）
1. **「好きなところでストップ」は存在しない。**
   手番は「全部振る → 最低1つ確定 → 残りを振り直す」を **全ダイスが確定するまで** 繰り返す。
   当初こちらが想定していた「任意のタイミングで確定してカードを選ぶ」設計は誤りだったので破棄。
   ダイスが多いほどターンが長くなり、目のコントロールが効かなくなる＝ジレンマの源泉。
2. カードは20種（LV0〜LVⅤ）。LV0 の道化師／ペテン師は **同一カードの表裏**。
   ペテン師は「道化師を持っているとき、どんな目でも取れる」＝道化師を裏返して置き換える。
3. 常時ダイス追加は 農夫+1 / ペテン師+1 / 将軍+2 の **最大7個**。
   これに手番中1回だけ使える「目がNの未確定ダイスを追加」
   （職人1・衛兵2・狩人3・質屋4・騎士5・司教6・王妃 任意）が最大7個乗って **理論値14個**。
   国王の7ゾロはこの積み上げで狙う。→ ダイス数は固定長ではなく可変長で持つ必要がある。
4. 効果の対象は原則 **「未確定のダイス」**。確定済みダイスを操作できるのは天文学者の参照元のみ。
   当初「確定済みも操作可にすると分かりやすい」と考えていたが、原典に従って未確定限定にする。
5. 出目判定は **排他的な分割ではなく包含判定**。原典に明記あり（1111 は「4つ同じ」かつ「2ペア」）。
6. 最終ラウンドの強さ順は ①同目数 ②目の大きさ ③先に振ったほう。

### 決めたこと（理由つき）
- **カード画像は英字IDにリネームして `assets/cards/` に置く。**
  日本語ファイル名はURLエンコードが絡んで取り回しが悪い。
  `assets/cards/${card.id}.jpg` で引ける規則にし、対応表を `assets/cards/README.md` に残した。
- **ソロ化は「制限ラウンド内に国王を獲得」＋スコア制。**
  原典にソロバリアントは無い（「オプション：ありません。」）ため独自設計。
  カード・出目条件・能力・最終ラウンドの強さ判定は原典のまま変えず、
  変えるのは「対戦相手の代わりに何を目標にするか」だけに絞った。
- **原典に無い判断は `99-faq.md` に集約して10件明文化した。**
  （貴族の+2を6に使えるか、哲学者・錬金術師の対象範囲、能力の使用タイミング等）
  実装中に暗黙で決めてしまうと後から根拠を追えなくなるため。

### 詰まった点 / 未解決
- 原典のルールHTMLは Shift_JIS。`iconv -f CP932` を通さないと文字化けする。
- `99-faq.md` の #2 #3 #9 #10（哲学者・錬金術師の対象範囲、能力の使用タイミング、
  道化師の再取得可否）は原典に記述がなく未解決。原典に当たれる機会があれば要確認。
- 実装コードは依然として未着手。次はフェーズ1。

---

## 2026-08-22 — ルール解釈3点の確定 / ゲーム本体を実装

### 利用者に確認して確定したこと（重要）
1. **手番はいつでも終えられる。**
   「アクティブなダイスから少なくとも1つを確定させる」＝**複数まとめて確定してよい**。
   残り全部を確定させればその時点で手番終了。前回のログで
   「好きなところでストップできない」と書いたのは読み違いだったので撤回した。
   → UIは「振り直しをやめる」ではなく「**残り◯個を全部確定（手番終了）**」というボタンにした。
2. 哲学者・錬金術師の対象は **未確定（アクティブ）のダイス限定**。
3. 道化師を裏返してペテン師にしたあと、**道化師は再取得できない**
   （通常プレイなら山に残っていれば可能だが、本アプリは全カード1枚プールのため）。
→ `docs/rules/99-faq.md` を「解決済み（利用者確認）」と「未解決」に分けて整理し直した。

### 実装したもの
- `src/game/` … `rng` / `requirements` / `cards` / `abilities` / `engine` / `score` / `storage`
- `src/ui/` … `render`（描画）/ `app`（状態保持・イベント配線・能力の対象選択フロー）
- `index.html` / `src/css/style.css` / `assets/favicon.svg`
- `test/` … 37件（`node --test` で全件パス）

### 決めたこと（理由つき）
- **能力の対象選択は「バナー＋ダイスクリック」方式。**
  能力ごとにダイアログを作ると11種類分の作りこみが必要になる。
  `abilitySpec()` が「何を選ばせるか」（mode）だけを返し、UI側はその mode に応じて
  ダイスのクリック挙動と追加の入力欄（目の選択／+1〜+3／和を保つ振り分け）を切り替える形にした。
- **哲学者・錬金術師の「和を変えずに振り分け」は ± ボタン方式。**
  合計が元と一致するまで確定ボタンを押せない。合計は常時表示して、崩れているときは色を変える。
- **獲得フェーズは専用モーダルを出さず、盤面下の「宮廷」から選ばせる。**
  実際のカード一覧から選ぶほうが原典に近く、条件も見比べられる。
  手番中も「今の出目なら何が取れるか」を同じ表示でハイライトしている（`previewClaimableIds`）。
- **`?debug` のときだけ `window.__kp` を生やす。**
  最終ラウンドやリザルトはランダム頼みでは再現しづらく、ヘッドレスブラウザでの
  通し確認に必要だった。本番の導線からは触れない。
- **`package.json` を追加した。** 依存パッケージのためではなく、
  `"type": "module"` が無いと Node がテストファイルの `import` を解釈できないため。

### 見つけて直した不具合
- `useAbility` が**カードの所持チェックをしていなかった**（持っていない能力が使えた）。
  テスト「持っていないカードの能力は使えない」が拾った。
- `replaceChildren(...)` に `null` を直接渡していて、画面に文字列 "null" が出ていた。
  スクリーンショットで発見。配列にして `.filter(Boolean)` するよう修正。
- CSS に無効な宣言（`border-color: #f4d display;`）が混入していたので削除。

### 確認したこと
- ヘッドレス Chromium で通し確認: 手番 → 確定 → 振り直し → 獲得 → 国王 → 最終ラウンド →
  リザルト（スコア内訳・自己ベスト判定）。コンソールエラー無し。
- 能力UI: 下女（+2）、哲学者（合計が崩れると確定不可になること）、王妃（目を選んで追加）。
- 375px幅で横スクロールが発生しないこと。

### 詰まった点 / 未解決
- 実機ブラウザでの目視は未実施。
- 難易度ごとの達成率を測っていないので、制限ラウンド数とスコア係数は暫定値。
